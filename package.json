{
    "name": "cinco-airbnb",
    "version": "0.2.0",
    "private": true,
    "scripts": {
        "preinstall": "echo --- preinstall --- && cd server && npm install && cd ../client && npm install && npm run build && cd ..",
        "start": "echo --- start --- && concurrently \"cd server && npm start\" \"cd client && npm start\"",
        "dev:server": "cd server && npm run dev",
        "dev:client": "cd client && npm run start"
    },
    "engines": {
        "node": "8.9.4"
    },
    "dependencies": {
        "concurrently": "^4.1.2"
    }
}